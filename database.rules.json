{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "metrics": {
      "$device_id": {
        ".indexOn": ["timestamp"]
      }
    },
    "running_processes": {
      "$device_id": {
        ".indexOn": ["lastUpdate"]
      }
    }
  }
}
